# 📂 ADMIN (my-ecommerce-admin)

cd my-ecommerce-admin

# Install React (latest)
npm install react@latest react-dom@latest

# Install additional frontend/backend support packages
npm install axios
npm install cookie-parser
npm install cors
npm install dotenv
npm install express
npm install fs
npm install jsonwebtoken
npm install multer
npm install path
And you come  pubic folder create new folder name uploads they store images.   Probably they create automatic  folder if you upload product.

# 📂 BACKEND

# Install backend dependencies
npm install express cors dotenv jsonwebtoken multer

# Install nodemon for development
npm install -D nodemon
